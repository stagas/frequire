module.exports = function (style) {
  var css = document.createElement('style')
  css.innerHTML = style
  function entry (target) {
    ;(target || document.head).appendChild(css)
  }
  entry.el = css
  entry.append = entry.insert = entry
  return entry
}
